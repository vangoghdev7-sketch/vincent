vincent_os:
  controllers:

    backend:
      type: deployment
      strategy: RollingUpdate
      rollingUpdate:
        unavailable: 1
      containers:
        main:
          securityContext:
            runAsUser: 1001
            runAsGroup: 1001
          image:
            pullPolicy: Always
            # GHCR (primary, public, auto-built on every push to main).
            # GitLab fallback: registry.gitlab.com/vangoghdev7-sketch/vincent/backend (requires auth + currently no CI builds it).
            repository: ghcr.io/vangoghdev7-sketch/vincent-backend
            tag: latest
          env:
            AIS_API_KEY:
              secretKeyRef:
                name: vincent_os-env
                key: AIS_API_KEY
            OPENSKY_CLIENT_ID:
              secretKeyRef:
                name: vincent_os-env
                key: OPENSKY_CLIENT_ID
            OPENSKY_CLIENT_SECRET:
              secretKeyRef:
                name: vincent_os-env
                key: OPENSKY_CLIENT_SECRET

    frontend:
      type: deployment
      strategy: RollingUpdate
      rollingUpdate:
        unavailable: 1
      containers:
        main:
          securityContext:
            runAsUser: 1001
            runAsGroup: 1001
          image:
            pullPolicy: Always
            # GHCR (primary, public, auto-built on every push to main).
            # GitLab fallback: registry.gitlab.com/vangoghdev7-sketch/vincent/frontend (requires auth + currently no CI builds it).
            repository: ghcr.io/vangoghdev7-sketch/vincent-frontend
            tag: latest

          env:
            BACKEND_URL: 'http://vincent_os-backend.vincent_os.svc.cluster.local:8000'

  service:
    backend:
      controller: backend
      type: ClusterIP
      ports:
        http:
          port: 8000
    frontend:
      controller: frontend
      type: ClusterIP
      ports:
        http:
          port: 3000
